Acione o subagente `ideias` para propor de 3 a 5 direções possíveis para o
próximo capítulo/cena, considerando o que já foi escrito em `/manuscrito/`
e o estado atual da bíblia em `/universo/`.

Argumento opcional do usuário (se fornecido, use como direcionamento):
$ARGUMENTS
